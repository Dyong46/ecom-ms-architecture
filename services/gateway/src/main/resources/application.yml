spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://locahost:9098/realms/micro-services"
  application:
    name: gateway-service
  config:
    import: configserver:http://localhost:8888